# Runtime dependencies
kivy>=2.3.0            # кроссплатформенный GUI
cryptography>=42.0     # AES-256-GCM шифрование локальной БД, PBKDF2
plyer>=2.1            # кроссплатформенные локальные уведомления (напоминания)
matplotlib>=3.8        # построение диаграмм для аналитики

# Dev / quality tools
pytest>=8.0            # модульные тесты
bandit>=1.7            # статический анализ безопасности (SAST)
pip-audit>=2.7         # проверка зависимостей на уязвимости
